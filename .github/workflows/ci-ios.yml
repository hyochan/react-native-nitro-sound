name: CI - iOS
on:
  workflow_call:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-ios:
    runs-on: macos-14
    env:
      XCODE_VERSION: 16.1
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup
        uses: ./.github/actions/setup

      - name: Install dependencies
        run: yarn install --immutable

      - name: Build library + generate nitrogen code
        run: yarn prepare

      - name: Use appropriate Xcode version
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: ${{ env.XCODE_VERSION }}

      - name: Cache CocoaPods
        id: pods-cache
        uses: actions/cache@v4
        with:
          path: example/ios/Pods
          key: ${{ runner.os }}-pods-${{ hashFiles('example/ios/Podfile.lock', 'example/ios/Podfile', 'example/Gemfile.lock', 'yarn.lock', 'NitroSound.podspec') }}

      - name: Setup Ruby + Bundler cache
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.2'
          bundler-cache: true
          working-directory: example

      - name: Install CocoaPods
        run: |
          cd example
          if [ "${{ steps.pods-cache.outputs.cache-hit }}" = "true" ]; then
            echo "Cache hit - reconciling the Pods sandbox with Podfile.lock"
            bundle exec pod install --project-directory=ios
          else
            echo "No cache hit - installing pods and refreshing specs"
            bundle exec pod install --project-directory=ios --repo-update
          fi

      - name: Build example for iOS
        run: |
          set +e
          xcodebuild \
            -workspace example/ios/SoundExample.xcworkspace \
            -scheme SoundExample \
            -configuration Debug \
            -sdk iphonesimulator \
            -destination 'generic/platform=iOS Simulator' \
            CODE_SIGNING_ALLOWED=NO \
            SKIP_BUNDLING=YES \
            build \
            2>&1 | tee /tmp/xcodebuild.log | grep -E "error:|warning:|BUILD (SUCCEEDED|FAILED)|▸"
          EXIT_CODE=${PIPESTATUS[0]}
          set -e
          if [ $EXIT_CODE -ne 0 ]; then
            echo "=== BUILD FAILED — last 50 raw lines ==="
            tail -50 /tmp/xcodebuild.log
          fi
          exit $EXIT_CODE
