{
  "name": "react-native-nitro-sound-plugin",
  "version": "1.0.0",
  "description": "Expo config plugin for react-native-nitro-sound",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "sync-version": "node scripts/sync-version.js",
    "build": "bun run sync-version && tsc",
    "prepare": "bun run build"
  },
  "keywords": [
    "expo",
    "config-plugin",
    "react-native",
    "audio",
    "recorder",
    "player"
  ],
  "author": "",
  "license": "MIT",
  "peerDependencies": {
    "expo": ">=50.0.0"
  },
  "dependencies": {
    "expo": ">=50.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.9.3"
  },
  "files": [
    "build",
    "README.md"
  ]
}
